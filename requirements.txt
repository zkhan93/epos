# Celery, Redis, eventlet, flower and dnspython are gone: crawls now run on a
# thread pool inside the web process, so there is no broker and no second
# container. See tasks.py.
Flask==3.1.3
flask-cors==6.0.5
gunicorn==26.1.0
requests==2.34.2
beautifulsoup4==4.15.0
diskcache==5.6.3
